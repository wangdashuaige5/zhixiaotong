# 软件工程数据、课堂签到与个人中心调整

更新日期：2026-09-15。本说明是本轮最新规则，覆盖早先文档中“签到须在当前时间窗内”“到时自动结束”的描述。

## 一、数据已实际导入

新增的是明确标记为“演示/示例”的大学软件工程数据，不代表真实学生、真实成绩或支付。没有清库，没有覆盖已有账号、密码、原课程及原签到记录。

| 内容 | 新增数量 |
| --- | ---: |
| 软件工程本科专业、2026级 | 各 1 |
| 四年制本科班级 | 2 |
| 虚构学生、教师 | 24、2 |
| 专业课程、教学班、课表记录 | 各 8 |
| 实验室 | 4 |
| 有效选课关系 | 162 |
| 作业、待批改提交 | 24、48 |
| 平时成绩草稿 | 54 |
| 图书书目、馆藏副本 | 12、36 |
| 专业选修批次 | 1 |
| 校园通知 | 5，配套站内消息 |

课程包括面向对象程序设计、数据结构与算法、数据库系统原理、软件需求工程、软件体系结构、软件测试与质量保证、移动应用开发、软件项目管理。课表经过教师、学生、教室冲突检查，教学班计数与选课名单一致。

原 student_demo、student_two、student_other 均增加六门必修演示课程，原身份和原选课不变；teacher_demo 负责前六门课程。新学生 se_student_01～se_student_24，每人同样有六门课程；新教师 se_teacher_01、se_teacher_02 分别负责移动应用开发、软件项目管理。新增账号密码为本项目已有演示口令 Demo@123456，仅用于本机演示；没有修改原账号密码。成绩是当前学期草稿，没有伪造已审核/已发布成绩。

数据由 SoftwareEngineeringDemoData 在 demo 环境且显式开启 campus.software-demo-seed=true 时导入，使用事务与一次性标记 SE_DEMO_2026，重复运行不会再次新增；正常运行配置已关闭该开关。代码包含导入数量、重复执行及课表一致性测试。不要删除该标记后重复导入，也不要把此开关用于真实生产数据。

导入前已完整备份数据库到本机工作区的 `智校通-本地备份` 目录，文件名为 `2026-09-15-before-software-demo.sql`。

该文件仅本人可读写，含原始业务数据与密码哈希，不要上传 GitHub 或共享。恢复整个备份会覆盖备份后的操作，需要维护者先评估，不能直接清库重导。

## 二、签到改为教师手动控制

1. 选择上课日期，再从该日期的真实课表选课程，仍使用 timetable_id，不手填 ID。
2. 开始/截止时间默认来自选中课程，时间选择器不以当前时分作为默认值。用户可修改；只校验同一天、截止晚于开始、时长小于 4 小时，不与当前时间比较。
3. 点击“发布 / 重新生成签到码”即开放。无论课程安排是过去还是未来，二维码不因为当前手机时间而隐藏，后端也不按当前时间拒绝签到。
4. 同一排课、同一天复用同一个任务，重新生成使旧码失效，但保留已有签到记录；不再插入重复任务而触发“数据已变化、重复或违反约束”。
5. 教师点击“结束本次签到”并确认后停止接收新签到；再次发布可重新开放。后台不再根据截止时间自动关闭，因此教师务必手动结束。
6. 学生仍须有有效选课、使用本次登录的可信设备及最新二维码。重复提交返回原记录，不新增重复签到；重复提交也不能绕过设备或二维码校验。
7. 服务器继续记录真实 sign_time，用于审计；不根据课程安排和当前时间自动推算迟到。原有迟到记录不会被改写。

新增接口：POST /api/attendance/tasks/{id}/close，仅有权任课教师可结束对应任务；原创建接口返回 task_status、reused。数据库表结构未变。

已在新增的“面向对象程序设计”演示课上完成真实 MySQL 联调，保留 1 条虚构学生签到记录；该联调任务最终已关闭。没有替真实学生签到，也没有修改原有签到任务。当前每排课/每天一份名单，不代表同一天多轮独立点名功能。

## 三、个人中心

所有角色采用一致的“身份卡片 → 基本资料 → 账号设置 → 退出登录”结构，角色使用中文名称，学生显示学号，其余角色显示工号。统一头像占位、留白、字号、卡片背景和按钮高度。

邮箱和密码表单默认收起，点击对应设置行才展开，避免页面被输入框占满；密码增加二次确认、前端强度检查、重复提交保护；切换编辑区/离开页面会清除密码输入。退出需要确认，改密成功清理旧凭据后回登录。

## 验证与限制

- 后端 62 项测试通过（0 失败、错误、跳过），新增数据与签到写操作先在隔离 H2 验证；新版后端已在本机 Java 17 容器运行并完成 MySQL 签到闭环检查。
- 前端 162 项逻辑测试通过：原交互32、签到25、会话/选课/审批20、分页40、教学32、个人中心/手动结束13。ArkTS 编译和 HAP 打包成功。
- 真实接口验证：重复发布 ID 不变、旧码拒绝、重复签到 ID 不变、学生不能结束教师任务、结束后未签到学生被拒绝、签到记录保留、新学生六门课程可读。
- 本轮没有实际扫描相机/相册，也未逐角色完成所有屏幕尺寸的人工视觉验收；不能把逻辑测试等同于这些设备验收。已保留原扫码实现，变更集中于业务时间规则与任务状态。
- 旧后端镜像保留为 zhixiaotong-demo-app:pre-software-enhance-20260915。未提交或推送 GitHub。
