# 智校通修复说明与验证

后续更新：软件工程数据已补充，课堂签到改为手动开放/结束，个人中心已重新布局。最新规则和数据数量见 [软件工程数据与签到调整说明](软件工程数据与签到调整说明.md)；本文记录之前一轮修复。

日期：2026-09-15。针对全功能检查中确认的问题，已按用户授权同时修改前端和后端。项目位置为桌面 `zhixiaotong`，不是旧的“小组后端代码-1”。未执行 Git 提交或推送；原有未提交改动已保留。

## 已完成

| 模块 | 本次修复与优化 |
| --- | --- |
| 登录和权限 | 访问凭据过期时自动刷新一次，并发请求共用刷新；刷新失效再回登录，网络错误不擅自清除账号。切换账号后旧请求不得覆盖新账号数据或重放操作。统一兼容后端权限对象数组，解决教师成绩功能误判无权限。 |
| 选课 | 自动读取当前开放批次，移除固定批次/课程 ID；选课选项与个人记录独立分页，退课确认、重复提交保护。无开放批次明确提示。 |
| 作业 | 按真实学期和本人教学班选课，教师用日期/时间选择器发布；学生提交、查看记录、退回后重交；教师查看姓名学号、评分、退回及发布。分数支持小数校验。 |
| 成绩 | 学生分页查分；教师按真实名册录入、保存草稿、提交审核；已发布成绩走更正申请；教学秘书查看快照并批准/驳回普通批次与更正申请。首次录分正确返回选课记录 ID。 |
| 审批权限 | 普通成绩批次和更正待办都排除本人申请，服务端仍禁止自审；组织与角色数据范围保持限制。 |
| 请假 | 草稿/退回申请可回填时间并修改；PUT 原申请保存或重新提交，轮次由服务端控制；审批人可通过、驳回、退回修改。显示申请人姓名学号；敏感操作二次确认。 |
| 课堂签到 | 保留课表联动的真实排课 ID 和日期/时间选择；教师生成二维码后可进入签到记录，按姓名/学号/状态分页查看。返回同一页面时不主动清除当前账号二维码，不将明文码写入持久化存储。 |
| 页面入口 | 补齐考试安排、教学秘书成绩审核入口；校园卡仅显示给学生，菜单保持去重。 |
| 列表分页 | 请假、考试、通知、消息、管理数据、图书及校园卡流水等增加翻页，兼容后端字符串 total；失败不保留误导性的旧列表。 |
| 图书与校园卡 | 图书增加本人预约、状态与取消；校园卡区分无权限/未绑卡，充值创建订单并查询服务端状态，不伪造支付或到账。 |
| 今日卡片 | 按权限分别取课程、消息和待办，辅导员/院系负责人不再因没有课表权限导致整个卡片失败。 |
| 排课及管理 | 教学班修改先校验已有排课的教师冲突、教室与容量，失败不产生部分更新；管理表单移除默认组织编号，增加数值校验与操作锁。 |
| 本机启动 | 启动脚本兼容独立 docker-compose，并提示 Docker 服务未启动；本机当前后端已切换修复版。 |

## 新增/扩展接口

- `GET /api/teaching-classes/mine`：本人任教或有效选课教学班，可选 semester_id。
- `GET /api/grade-changes/pending`：当前授权范围、排除本人申请的成绩更正待办。
- `GET /api/grade-changes/{id}`：申请者或有权审核人的更正详情。
- 作业提交列表、签到记录、请假待办补姓名与学号；普通成绩待办补教学班/课程/提交人/快照展示信息。

没有新增数据库迁移、没有重置种子数据。所有写入仍由后端核对权限、状态、版本或审批轮次。

## 验证结果

- 前端逻辑测试 **148 项通过**：原交互 32、签到日期/课表 24、会话/选课/审批 20、分页/生活服务/请假 40、教学工作流 32。
- 前端完整 ArkTS 编译及 HAP 打包成功；保持原 API 18 目标。产物未签名，未安装到设备。已有 router/promptAction 等弃用警告不等于编译失败，本次未做无关的全项目 API 迁移。
- 后端在隔离 **Java 17.0.15** 环境下运行 **56 项测试**，0 失败、0 错误、0 跳过，并成功打包。写操作测试用隔离 H2 数据，不挂载当前 MySQL。
- 本机新后端用 8 个演示角色进行 **86 次接口检查**，全部符合预期，含应拒绝的无权限请求；独立测试会话均退出。业务接口只读，不代用户发布、选课、审批、签到、充值或借阅。
- 新版服务健康检查 UP；运行包 SHA-256 为 `fcd625d0e4c2e876ca50a0ea58fb3c0f99d2cd3a160974de028bfdd0a337548c`，项目 `houduan/dist` 已同步。
- 更新前后用户、选课、作业、成绩、请假、签到任务/记录、校园卡流水、借阅以及成功迁移数量一致；数据库容器及文件卷没有重建。
- 源码格式差异检查通过。构建和接口结果保存在工作区 `智校通-修复验证-2026-09-15`。

本机可在 `qianduan` 下分别执行以下测试（其他电脑用 TYPESCRIPT_PATH 指向本地 TypeScript）：

```sh
TZ=Asia/Shanghai node scripts/test-frontend.cjs
TZ=Asia/Shanghai node scripts/test-attendance-date.cjs
TZ=Asia/Shanghai node scripts/test-session-enrollment.cjs
TZ=Asia/Shanghai node scripts/test-pagination-life.cjs
TZ=Asia/Shanghai node scripts/test-teaching-workflows.cjs
```

## 使用与尚未验收的部分

1. DevEco 打开桌面 `zhixiaotong/qianduan`，重新运行前端；不要继续运行旧安装包。本机后端仍只监听 `127.0.0.1:8080`，没有开放到外网。
2. 本次末次检查设备列表为空。连接模拟器后检查 `hdc -t <设备标识> rport tcp:8080 tcp:8080` 转发，再按学生、教师、教学秘书、辅导员分别验收页面。尚未验证相机、相册识别和手机尺寸布局，逻辑测试不能替代这些验收。
3. 当前演示学生没有有效选课时，课表/作业/签到不可用是业务前置条件，不是端口故障。本次没有擅自选课或造签到记录。需在开放批次正常选课，或由有权管理员配置业务数据。
4. 真实支付、短信、校园卡/图书外部系统、附件上传界面、完整管理员业务表单、自动缺勤统计仍不宣称完成。当前签到记录仅列已有记录，不推算未签到者为缺勤；静态二维码也不能证明人在教室。
5. 二维码跨页面销毁或应用重启后的恢复、动态防转发码等，需要额外设计；当前后端只保存哈希，不能从哈希恢复原明文码。

## 回退信息

应用容器已重建为新版，旧后端镜像保留为 `zhixiaotong-demo-app:pre-repair-20260915-gych8h`；数据库容器和数据卷未删除。旧 JAR 同时保存在本轮临时验证目录的 `backend-before-repair.jar`（临时目录并非长期备份）。如需回退，可由维护者将旧标签重新标记为应用镜像并仅重建 app 服务；不需要删除数据库或执行清库命令。
